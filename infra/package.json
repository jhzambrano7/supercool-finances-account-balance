{
  "name": "account-balance-infra",
  "private": true,
  "version": "0.1.0",
  "description": "The CDK CLI only. The infrastructure itself is Python (see pyproject.toml's `infra` dependency group) — AWS publishes no CLI to PyPI, so this is the one npm dependency a Python CDK project cannot avoid.",
  "devDependencies": {
    "aws-cdk": "^2.174.0"
  }
}
